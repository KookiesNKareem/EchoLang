# Optional: USB-mic audio capture + whisper.cpp transcription.
# Only needed if you want live audio transcription (vs typed-caption mode).
# Requires `sudo apt-get install -y portaudio19-dev libsndfile1` first.
sounddevice==0.5.1
pywhispercpp==1.3.0
