# Core server deps — pure Python or pre-built wheels on Pi 5 ARM64.
# Ollama backend is the default and needs no native compilation here.
fastapi==0.115.0
uvicorn[standard]==0.32.0
sse-starlette==2.1.3
pydantic==2.9.2
pydantic-settings==2.6.1
python-multipart==0.0.12
numpy==2.1.3
# Note: mDNS advertising uses the Pi OS `avahi-publish-service` system tool
# (sudo apt-get install -y avahi-utils). We deliberately don't use the
# Python `zeroconf` package because it conflicts with the running Avahi
# daemon for the multicast group socket.

